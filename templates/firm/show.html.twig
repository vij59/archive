{% extends 'base.html.twig' %}

{% block title %}Firm{% endblock %}

{% block body %}
{#    <h1>Entreprise</h1>#}

{#    <label for="meeting-time">Archive au temps T :</label>#}

{#    <form>#}
{#        <label for="party">Veuillez choisir une date et une heure pour l'archive :</label>#}
{#        <input id="alert"  type="datetime-local" name="partydate">#}
{#    </form>#}

    {{ form_start(form) }}
        {{ form_widget(form) }}
    <button class="btn btn-primary" type="submit">Valider </button>
    {{ form_end(form) }}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ firm.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ firm.name }}</td>
            </tr>
            <tr>
                <th>Siren</th>
                <td>{{ firm.siren }}</td>
            </tr>
            <tr>
                <th>ImmatriculationCity</th>
                <td>{{ firm.immatriculationCity }}</td>
            </tr>
            <tr>
                <th>ImmatriculationDate</th>
                <td>{{ firm.immatriculationDate ? firm.immatriculationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Capital</th>
                <td>{{ firm.capital }}</td>
            </tr>
            <tr>
                <th>Addresses</th>
                <td>
                    {% for address in firm.addresses %}
                        {{ address.number }}
                        {{ address.wayType }}
                        {{ address.wayName }}
                        {{ address.city }}
                        {{ address.zipCode }}
                        <br/>
                    {% endfor %}
                </td>
            </tr>
            <tr>
                <th>Snapshot</th>
                <td>

                       <pre>
</pre>

{#                    {% for snap in firm.snapshots %}#}
{#                        {{ dump(snap.modificationDateTime) }}#}
{#                        <a href="/snapshot/{{ snap.id }}">#}
{#                      Archive du#}
{#                        {{ snap.modificationDateTime ? snap.modificationDateTime |date('Y-m-d H:i:s') : '' }}#}
{#                        </a>#}
{#<br/>#}
{#                    {% endfor %}#}

                </td>

            </tr>
        </tbody>
    </table>

    <p id="regTitle"></p>

    <a href="{{ path('firm_index') }}">Retour à la liste</a>

    <a href="{{ path('firm_edit', {'id': firm.id}) }}">Modifier</a>

    {{ include('firm/_delete_form.html.twig') }}
{% endblock %}

 {% block javascripts %}

     <script src="/jquery/jquery-3.4.1.min.js"></script>
     <script src = "https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>

     <script>
         $('.datepicker').datepicker();
     </script>

 {% endblock %}